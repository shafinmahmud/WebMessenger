(function (angular) {
    angular.module("chatApp", [
        "chatApp.controller",
        "chatApp.service"
    ]);
    angular.module("chatApp.controller", []);
    angular.module("chatApp.service", []);
})(angular);